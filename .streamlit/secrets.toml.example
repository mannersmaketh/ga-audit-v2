# Google OAuth 2.0 Credentials
# Replace these with your actual OAuth credentials from Google Cloud Console
client_id = "your-oauth-client-id-here"
client_secret = "your-oauth-client-secret-here"

# Instructions:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable Google Analytics Data API and Google Sheets API
# 4. Go to "APIs & Services" > "Credentials"
# 5. Create OAuth 2.0 Client ID (Web application)
# 6. Add authorized redirect URI: http://localhost:8501 (for local development)
# 7. Copy the client ID and client secret here 